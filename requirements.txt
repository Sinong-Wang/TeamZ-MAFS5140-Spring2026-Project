# 回测框架 (main.py / engine / data_feed)
numpy>=1.24,<3
pandas>=2.0,<3
pyarrow>=12
fastparquet>=2024.0  # 部分 Parquet 与 PyArrow 不兼容时 data_feed 会回退尝试

# LightGBM 实验 notebook + lgb_data_pipeline
polars>=0.20
joblib>=1.3
lightgbm>=4.0
scikit-learn>=1.3

# Notebook 绘图与其它作业
matplotlib>=3.7
scipy>=1.10
requests>=2.28

# 运行 .ipynb
jupyter>=1.0
ipykernel>=6.0
